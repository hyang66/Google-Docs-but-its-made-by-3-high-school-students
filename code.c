            ch = getch();
            if (ch == 'Q' || ch == 'D' || ch == 'U' || ch == 0xa){
                break;
            }



if (ch == 'Q') {
        exit(Q);
} 
if (ch == 'U') {
        exit(UP);
} 
if (ch == 'D') {
        exit(DOWN);
} 
if (ch == 0xa) {
        exit(ENTER);
} 


                     { KEY_ENTER,     "ENTER"         },

