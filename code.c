            ch = getch();
            if (ch == 'q' || ch == 'D' || ch == 'U'){
                break;
            }



if (ch == 'Q') {
        exit(Q);
} 
if (ch == 'U') {
        exit(UP);
} 
if (ch == 'D') {
        exit(DOWN);
} 


                     { KEY_ENTER,     "ENTER"         },

