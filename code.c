            ch = getch();
            if (ch == 'Q' || ch == 'D' || ch == 'U' || ch == 0xa){
                break;
            }



if (ch == 0x1b) {
        exit(Q);
} 
if (ch == 0x103) {
        exit(UP);
} 
if (ch == 0x102) {
        exit(DOWN);
} 
if (ch == 0xa) {
        exit(ENTER);
} 


                     { KEY_ENTER,     "ENTER"         },

